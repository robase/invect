# oxlint reads this file (ESLint's traditional ignore file). Patterns here
# apply globally regardless of nested config presence.
.claude/
.claude/worktrees/
node_modules/
dist/
.next/
coverage/
build/
.turbo/
drizzle/
docs/
# Codegen output — regenerated by `pnpm --filter @invect/sdk gen-actions`.
pkg/sdk/src/generated/
