// AUTOGENERATED — do not edit. Run `pnpm --filter @invect/sdk gen-actions`.
// Source: `@invect/actions/google-drive` action catalogue.

import {
  googleDriveCreateFileAction as _googleDriveCreateFileAction,
  googleDriveCreateFolderAction as _googleDriveCreateFolderAction,
  googleDriveDeleteFileAction as _googleDriveDeleteFileAction,
  googleDriveGetFileAction as _googleDriveGetFileAction,
  googleDriveListFilesAction as _googleDriveListFilesAction,
  googleDriveSearchFilesAction as _googleDriveSearchFilesAction,
  googleDriveShareFileAction as _googleDriveShareFileAction,
} from '@invect/actions/google-drive';
import type { z } from 'zod/v4';
import type { NodeOptions, SdkFlowNode } from '@invect/action-kit';

export interface GoogleDriveCreateFileParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveCreateFileAction.params.schema>['credentialId'];
  /**
   * Name of the file to create
   * @example report.txt
   */
  name: z.input<typeof _googleDriveCreateFileAction.params.schema>['name'];
  /**
   * Text content of the file
   * @example File content here...
   */
  content?: z.input<typeof _googleDriveCreateFileAction.params.schema>['content'];
  /**
   * MIME type (e.g. text/plain, text/csv, application/json)
   * @defaultValue "text/plain"
   */
  mimeType?: z.input<typeof _googleDriveCreateFileAction.params.schema>['mimeType'];
  /**
   * ID of the parent folder (root if empty)
   * @example folder-id-here
   */
  parentFolderId?: z.input<typeof _googleDriveCreateFileAction.params.schema>['parentFolderId'];
  /** File description */
  description?: z.input<typeof _googleDriveCreateFileAction.params.schema>['description'];
}

/**
 * Create a new text-based file in Google Drive (files.create with multipart upload). Use when the user wants to upload or create a file with content. Call with `name` and `content`; optional `mimeType` (default text/plain), `parentFolderId`, and `description`.
 *
 * Example response:
 * ```json
 * {"id": "1abc", "name": "report.txt", "mimeType": "text/plain", "size": "256", "webViewLink": "https://drive.google.com/file/d/1abc/view"}
 * ```
 */
export function createFile(
  params: GoogleDriveCreateFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.create_file'>;
export function createFile(
  referenceId: string,
  params: GoogleDriveCreateFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.create_file'>;
export function createFile(
  ...args:
    | [params: GoogleDriveCreateFileParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveCreateFileParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.create_file'> {
  return (
    _googleDriveCreateFileAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.create_file'>
  )(...args);
}

export interface GoogleDriveCreateFolderParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveCreateFolderAction.params.schema>['credentialId'];
  /**
   * Name of the folder to create
   * @example My Folder
   */
  name: z.input<typeof _googleDriveCreateFolderAction.params.schema>['name'];
  /**
   * ID of the parent folder (root if empty)
   * @example parent-folder-id
   */
  parentFolderId?: z.input<typeof _googleDriveCreateFolderAction.params.schema>['parentFolderId'];
  /** Folder description */
  description?: z.input<typeof _googleDriveCreateFolderAction.params.schema>['description'];
}

/**
 * Create a new folder in Google Drive (files.create with folder mimeType). Use when the user wants to organize files into a new folder. Call with `name`; optional `parentFolderId` to nest under an existing folder, and `description`.
 *
 * Example response:
 * ```json
 * {"id": "1abc", "name": "My Folder", "mimeType": "application/vnd.google-apps.folder", "webViewLink": "https://drive.google.com/drive/folders/1abc"}
 * ```
 */
export function createFolder(
  params: GoogleDriveCreateFolderParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.create_folder'>;
export function createFolder(
  referenceId: string,
  params: GoogleDriveCreateFolderParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.create_folder'>;
export function createFolder(
  ...args:
    | [params: GoogleDriveCreateFolderParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveCreateFolderParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.create_folder'> {
  return (
    _googleDriveCreateFolderAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.create_folder'>
  )(...args);
}

export interface GoogleDriveDeleteFileParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveDeleteFileAction.params.schema>['credentialId'];
  /** The ID of the file to delete */
  fileId: z.input<typeof _googleDriveDeleteFileAction.params.schema>['fileId'];
}

/**
 * Permanently delete a file from Google Drive (files.delete). Use when the user wants to remove a file — this cannot be undone. Call with `fileId` of the file to delete. The API returns 204 No Content; this action returns a confirmation object.
 *
 * Example response:
 * ```json
 * {"fileId": "1abc", "deleted": true}
 * ```
 */
export function deleteFile(
  params: GoogleDriveDeleteFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.delete_file'>;
export function deleteFile(
  referenceId: string,
  params: GoogleDriveDeleteFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.delete_file'>;
export function deleteFile(
  ...args:
    | [params: GoogleDriveDeleteFileParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveDeleteFileParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.delete_file'> {
  return (
    _googleDriveDeleteFileAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.delete_file'>
  )(...args);
}

export interface GoogleDriveGetFileParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveGetFileAction.params.schema>['credentialId'];
  /** The ID of the file to retrieve */
  fileId: z.input<typeof _googleDriveGetFileAction.params.schema>['fileId'];
}

/**
 * Retrieve metadata of a file in Google Drive by its ID (files.get). Use when the user wants to get details about a specific file such as name, size, MIME type, and view link. Call with `fileId` to retrieve file metadata.
 *
 * Example response:
 * ```json
 * {"id": "1abc", "name": "Report.pdf", "mimeType": "application/pdf", "size": "12345", "webViewLink": "https://drive.google.com/file/d/1abc/view"}
 * ```
 */
export function getFile(
  params: GoogleDriveGetFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.get_file'>;
export function getFile(
  referenceId: string,
  params: GoogleDriveGetFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.get_file'>;
export function getFile(
  ...args:
    | [params: GoogleDriveGetFileParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveGetFileParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.get_file'> {
  return (
    _googleDriveGetFileAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.get_file'>
  )(...args);
}

export interface GoogleDriveListFilesParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveListFilesAction.params.schema>['credentialId'];
  /**
   * Google Drive search query syntax. Leave empty to list all files.
   * @example name contains 'report' and mimeType='application/pdf'
   */
  query?: z.input<typeof _googleDriveListFilesAction.params.schema>['query'];
  /**
   * Limit results to a specific folder by ID
   * @example folder-id-here
   */
  folderId?: z.input<typeof _googleDriveListFilesAction.params.schema>['folderId'];
  /**
   * Maximum number of files to return (1–1000)
   * @defaultValue 20
   */
  maxResults?: z.input<typeof _googleDriveListFilesAction.params.schema>['maxResults'];
  /**
   * Sort order (e.g. 'modifiedTime desc', 'name')
   * @defaultValue "modifiedTime desc"
   */
  orderBy?: z.input<typeof _googleDriveListFilesAction.params.schema>['orderBy'];
  /**
   * Specific fields to include in response
   * @defaultValue "files(id,name,mimeType,size,modifiedTime,webViewLink,parents)"
   */
  fields?: z.input<typeof _googleDriveListFilesAction.params.schema>['fields'];
}

/**
 * List files and folders in Google Drive (files.list). Use when the user wants to browse their Drive, list recent files, or filter by folder. Call with optional `query` (Drive search syntax, e.g. "name contains 'report'"), `folderId` to limit to a specific folder, and `maxResults` to control page size.
 *
 * Example response:
 * ```json
 * {"files": [{"id": "1abc", "name": "Report.pdf", "mimeType": "application/pdf", "modifiedTime": "2025-03-15T10:00:00Z"}], "fileCount": 1, "hasMore": false}
 * ```
 */
export function listFiles(
  params: GoogleDriveListFilesParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.list_files'>;
export function listFiles(
  referenceId: string,
  params: GoogleDriveListFilesParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.list_files'>;
export function listFiles(
  ...args:
    | [params: GoogleDriveListFilesParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveListFilesParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.list_files'> {
  return (
    _googleDriveListFilesAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.list_files'>
  )(...args);
}

export interface GoogleDriveSearchFilesParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveSearchFilesAction.params.schema>['credentialId'];
  /**
   * Google Drive search query syntax
   * @example name contains 'report' and mimeType='application/pdf'
   */
  searchQuery: z.input<typeof _googleDriveSearchFilesAction.params.schema>['searchQuery'];
  /**
   * Maximum number of results (1–1000)
   * @defaultValue 20
   */
  maxResults?: z.input<typeof _googleDriveSearchFilesAction.params.schema>['maxResults'];
  /**
   * Include files from shared drives in results
   * @defaultValue false
   */
  supportsAllDrives?: z.input<
    typeof _googleDriveSearchFilesAction.params.schema
  >['supportsAllDrives'];
}

/**
 * Search for files in Google Drive using query syntax (files.list with q parameter). Use when the user wants to find files by name, type, or other criteria. Call with `searchQuery` using Drive search syntax (e.g. "name contains 'report' and mimeType='application/pdf'"); optional `maxResults` and `supportsAllDrives`.
 *
 * Example response:
 * ```json
 * {"files": [{"id": "1abc", "name": "Report.pdf", "mimeType": "application/pdf", "modifiedTime": "2025-03-15T10:00:00Z"}], "fileCount": 1, "hasMore": false}
 * ```
 */
export function searchFiles(
  params: GoogleDriveSearchFilesParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.search_files'>;
export function searchFiles(
  referenceId: string,
  params: GoogleDriveSearchFilesParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.search_files'>;
export function searchFiles(
  ...args:
    | [params: GoogleDriveSearchFilesParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveSearchFilesParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.search_files'> {
  return (
    _googleDriveSearchFilesAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.search_files'>
  )(...args);
}

export interface GoogleDriveShareFileParams {
  /** Google Drive OAuth2 credential for authentication */
  credentialId: z.input<typeof _googleDriveShareFileAction.params.schema>['credentialId'];
  /** The ID of the file to share */
  fileId: z.input<typeof _googleDriveShareFileAction.params.schema>['fileId'];
  /** Permission level to grant */
  role: z.input<typeof _googleDriveShareFileAction.params.schema>['role'];
  /** Type of permission */
  type: z.input<typeof _googleDriveShareFileAction.params.schema>['type'];
  /**
   * Email address (required for user/group type)
   * @example user@example.com
   */
  emailAddress?: z.input<typeof _googleDriveShareFileAction.params.schema>['emailAddress'];
  /**
   * Domain name (required for domain type)
   * @example example.com
   */
  domain?: z.input<typeof _googleDriveShareFileAction.params.schema>['domain'];
  /**
   * Whether to send a notification email to the recipient
   * @defaultValue true
   */
  sendNotificationEmail?: z.input<
    typeof _googleDriveShareFileAction.params.schema
  >['sendNotificationEmail'];
}

/**
 * Share a file in Google Drive by creating a permission (permissions.create). Use when the user wants to grant access to a file for a user, group, domain, or publicly. Call with `fileId`, `role` (reader/commenter/writer/organizer), `type` (user/group/domain/anyone), and `emailAddress` for user/group sharing.
 *
 * Example response:
 * ```json
 * {"permissionId": "12345", "fileId": "1abc", "role": "writer", "type": "user", "emailAddress": "user@example.com"}
 * ```
 */
export function shareFile(
  params: GoogleDriveShareFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.share_file'>;
export function shareFile(
  referenceId: string,
  params: GoogleDriveShareFileParams,
  options?: NodeOptions,
): SdkFlowNode<string, 'google_drive.share_file'>;
export function shareFile(
  ...args:
    | [params: GoogleDriveShareFileParams, options?: NodeOptions]
    | [referenceId: string, params: GoogleDriveShareFileParams, options?: NodeOptions]
): SdkFlowNode<string, 'google_drive.share_file'> {
  return (
    _googleDriveShareFileAction as (
      ...args: unknown[]
    ) => SdkFlowNode<string, 'google_drive.share_file'>
  )(...args);
}
