services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: acme
      POSTGRES_PASSWORD: acme
      POSTGRES_DB: acme_dashboard
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
